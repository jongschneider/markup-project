extends layout

include mixins/_searchForms.pug

block content
  +searchForms()
  //- form(action='/getscores', method="GET")
  //-   select(name='select')
  //-     option(default=true disabled=true) Choose a File
  //-     option(value='All Scores') All
  //-     each file in data
  //-       option(value=file) #{file}
  //-   button(type='submit', name='score' value='score') Search Scores
  //-   
  //- form(action='/getScoresByKey', method="GET")
  //-   select(name='select')
  //-     option(default=true disabled=true) Choose a Key
  //-     each key in keyList
  //-       option(value=key) #{key}
  //-   button(type='submit', name='score' value='score') Search Scores

  h1 #{title}
  table.table.table-striped
    caption List of All Files
    thead
      tr
        th Date
        th File Name
        th Score
    tbody
      each row in results
        tr
          td #{row.score_runtime}
          td #{row.html_filenames_html_filename}
          td #{row.score}
        tr
  h3 AVG #{avg[0].avg}
