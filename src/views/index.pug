extends layout

block content
  //- form(action="/getscores", method="POST")
  //-   h2=
  //- form(id="", method="", action="")
  //-   input(type="text", placeholder="placeholder")
  //-   textarea(cols="50", rows="100")
  //-     | #{data}

  form(action='/scoreFile', method="GET")
    select(name='select')
      option(default=true ) Choose a File
      each file in data
        option(value=file) #{file}
    button(type='submit', name='score' value='score') Score File
  div(style = "white-space:PRE")
    | #{html}

  
